const calculator = require('./calculator');
const utils = require('./utils');

console.log('Application loaded successfully');
console.log('Calculator and Utils modules are available');

module.exports = {
  calculator,
  utils
};
